PP_l=${PP_l:-}
EP=${EP:-1}
if [ -z "$PP_l" ]; then DP_OVERLAP=${DP_OVERLAP:-0};
else DP_OVERLAP=${DP_OVERLAP:-1}; fi
CLONE_SCATTER_OUTPUT_IN_EMBEDDING=${CLONE_SCATTER_OUTPUT_IN_EMBEDDING:-0}
POST_LM_PROCESSING_SLICE_SIZE=${POST_LM_PROCESSING_SLICE_SIZE:-0}
SFT_PADDING=${SFT_PADDING:-0}
if [ $CLUSTER == h800 ]; then CLUSTER_DP_CTAS_MULTIPLIER=2;
else CLUSTER_DP_CTAS_MULTIPLIER=1; fi
